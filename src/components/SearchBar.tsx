const SearchBar = () => {
  return (
    <input
      className=" px-5 py-1 w-2/3 sm:px-5 sm:py-3 flex-1 text-zinc-200 bg-zinc-800 rounded-xl"
      placeholder="What Pokemon are you looking for?"
    />
  );
};

export default SearchBar;
